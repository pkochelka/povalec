# Compatible with Python 3.10 through 3.14.
#
# These are the project's direct dependencies, not a full `pip freeze`. Versions
# are given as floors (old enough to have wheels on 3.10) with no upper caps, so
# pip selects the newest build that has wheels for your interpreter - e.g. an
# older numpy/scipy on 3.10 and a newer one on 3.14, automatically.
#
# GPU note: this installs the default torch build from PyPI. For a specific CUDA
# build, install torch first from the PyTorch index, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128

# --- Data, ML, training, inference ---
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=14
torch>=2.2
transformers>=4.46
accelerate>=1.1.0
peft>=0.17          # LoRA fine-tuning in analysis/train_ideo_encoder.py
datasets>=2.19
sentencepiece>=0.2.0
protobuf>=4.21

# --- HTTP, config, progress ---
requests>=2.31
httpx>=0.27
python-dotenv>=1.0
tqdm>=4.66

# --- EU&I / RDF parsing and CLI ---
beautifulsoup4>=4.12
rdflib>=7.0
jsonargparse>=4.27

# --- Dataset preprocessing only ---
# Used solely by "data/EuroParl Custom/{downloads,lid}.py"; the analysis,
# training and inference scripts do not import these. They are the main
# portability hazards across Python versions, so install them only if you are
# rebuilding the dataset:
#   - fasttext builds from C++ source and lacks official wheels for recent
#     Python; on 3.12+ prefer the prebuilt 'fasttext-wheel'.
#   - trankit pins older transformers/torch and may downgrade them; consider a
#     separate environment if it conflicts with the versions above.
# fasttext-wheel>=0.9.2
# trankit>=1.1.0
