# The dependency list lives in pyproject.toml, and the project is managed with uv:
#
#   uv venv --python 3.11
#   uv pip install -e .            # or -e ".[lid]" for the fastText language-ID step
#
# `uv.lock` pins the exact resolution; `uv sync` reproduces it.
#
# This file stays only so that a plain `pip install -r requirements.txt` still works.
# Prefer uv, and never install this project into a system interpreter.
-e .
