# Compatible with Python 3.10 through 3.14.
#
# These are the project's direct dependencies, not a full `pip freeze`. Versions
# are given as floors (old enough to have wheels on 3.10) with no upper caps, so
# pip selects the newest build that has wheels for your interpreter - e.g. an
# older numpy/scipy on 3.10 and a newer one on 3.14, automatically.
#
# GPU note: this installs the default torch build from PyPI. For a specific CUDA
# build, install torch first from the PyTorch index, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128

# --- Data, ML, training, inference ---
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=14
torch>=2.2
transformers>=4.46
accelerate>=1.1.0
datasets>=2.19
# Not imported directly: sentencepiece and protobuf are the tokenizer backends
# transformers loads at runtime for the mmBERT / XLM-R style models used here.
sentencepiece>=0.2.0
protobuf>=4.21

# --- HTTP, config, progress ---
requests>=2.31
httpx>=0.27
python-dotenv>=1.0
tqdm>=4.66

# --- EU&I scraping ---
beautifulsoup4>=4.12

# --- Corpus rebuild only ---
# Needed only to rebuild "data/EuroParl Custom" from scratch via
# preprocessing/{fetch_raw_data,europarl_rdf_query,europarl_lid_filter}.py.
# The analysis, training and inference scripts do not import them.
#   - huggingface_hub also arrives with datasets/transformers above; listed
#     here because fetch_raw_data.py imports it directly.
#   - rdflib is used solely by europarl_rdf_query.py.
rdflib>=7.0
huggingface_hub>=0.24
#
# fastText is needed by europarl_lid_filter.py alone, and is the main
# portability hazard here, so it is left commented out. The script accepts
# either binding: 'fasttext-wheel' is prebuilt through 3.12, 'fasttext-predict'
# is prediction-only with 3.13 wheels, and plain 'fasttext' builds from C++
# source. None of them installs on 3.14 yet - run that one step on 3.10-3.13.
# fasttext-wheel>=0.9.2
# fasttext-predict>=0.9.2
